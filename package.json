{
  "name": "my-react-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://shaunwa.github.io/my-react-app",
  "dependencies": {
    "@testing-library/jest-dom": "4.2.4",
    "@testing-library/react": "9.5.0",
    "@testing-library/user-event": "7.2.1",
    "query-string": "6.12.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-router-dom": "5.1.2",
    "react-scripts": "3.4.1",
    "styled-components": "5.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy:netlify": "npm run build && netlify deploy --dir ./build",
    "deploy:s3": "npm run build && aws s3 sync build/ s3://shauns-react-app",
    "deploy:firebase": "npm run build && firebase deploy",
    "deploy:github": "npm run build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "3.0.0",
    "netlify-cli": "2.53.0"
  }
}
